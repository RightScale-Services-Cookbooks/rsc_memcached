LoadPlugin memcached

<Plugin "memcached">
    Host <%= @options[:host] %>
    Port <%= @options[:port] %>
</Plugin>

LoadPlugin exec
<Plugin exec>
  Exec "<%= @options[:user] %>" "<%= @options[:location] %>" "-h" "<%= @options[:uuid] %>"
</Plugin>
